# Django Settings
DEBUG=True
SECRET_KEY=django-insecure-car-hire-dev-key-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (PostgreSQL)
DATABASE_URL=postgresql://postgres:1234@localhost:5432/car_hire_system
# Fallback to SQLite for development if PostgreSQL not available
# DATABASE_URL=sqlite:///db.sqlite3

# Redis Cache (will add later when needed)
REDIS_URL=redis://localhost:6379/0

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440

# File Upload
MEDIA_ROOT=media/
MEDIA_URL=/media/
